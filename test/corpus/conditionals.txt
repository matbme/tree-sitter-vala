=============
If statement
=============

if (i > 0) {
    // ...
}
else {
    // ...
}

---

(source_file
    (if_statement
        (binary_expression (identifier) (number))
        (block (comment))
        (block (comment))
    )
)


=================
Switch statement
=================

switch (a) {
    case 1:
        foo ();
        break;
    case 2:
    default:
        bar ();
        break;
}

---

(source_file
    (switch_statement (identifier) (switch_block
        (case (number)
            (function_call (identifier) (parameter_list))
            (break_statement)
        )
        (case (number))
        (case
            (function_call (identifier) (parameter_list))
            (break_statement)
        )
    ))
)


===================
Ternary expression
===================

string sig = a > 0 ? "positive" : "negative";

---

(source_file
    (declaration (primitive_type) (identifier) (ternary_expression
        (binary_expression (identifier) (number))
        (string_literal)
        (string_literal)
    ))
)
